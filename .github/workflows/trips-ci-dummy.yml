name: trips-ci

on:
  pull_request:
    branches: [main]
    paths-ignore:
      - "apis/trips/**"
jobs:
  build:
    name: trips-ci-job
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required" '
